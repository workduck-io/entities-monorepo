import { initializeEntity } from '@mex/entity-utils';
import { <%= name %>Table } from '../service/DynamoDB';

export const <%= pascal(name) %>Entity = initializeEntity({
  name: '<%= name %>',
  additionalAttributes: {
    blockId: { type: 'string', required: 'always' },
  },
  table: <%= name %>Table,
} as const);
